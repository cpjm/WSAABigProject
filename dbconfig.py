# Note, this is a dummy db config file.
# This will be changed on the web server itself once hosted.
# Avoids the real DB login info from being published on github.
# Yet also facilitates the (dummy) file itself existing in the Github repo.
mysql = {
    'host':"localhost",
    'user':"root",
    'password':"",
    'database':"datarepresentation"
}